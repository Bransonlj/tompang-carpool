name: Carpool Service CI

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'carpool-service/**'
      - '.github/workflows/carpool-ci.yml'
      - 'schemas/**'
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'carpool-service/**'
      - '.github/workflows/carpool-ci.yml'
      - 'schemas/**'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Java 17
      uses: actions/setup-java@v3
      with:
        distribution: temurin
        java-version: 17
        
    - name: Make Maven wrapper executable
      run: chmod +x ./mvnw
      
    - name: Cache Maven dependencies
      uses: actions/cache@v3
      with:
        path: ~/.m2/repository
        key: ${{ runner.os }}-maven-${{ hashFiles('carpool-service/pom.xml') }}
        restore-keys: |
          ${{ runner.os }}-maven-

    - name: Build and Test Carpool Service
      run: |
        cd carpool-service
        ./mvnw clean verify
